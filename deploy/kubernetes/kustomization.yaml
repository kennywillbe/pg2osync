apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pg2osync

resources:
  - namespace.yaml
  - secret.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  # - servicemonitor.yaml   # needs the Prometheus Operator CRDs
  # - poddisruptionbudget.yaml   # blocks node drains until you move the pod

images:
  - name: ghcr.io/kennywillbe/pg2osync
    newTag: 1.5.0 # x-release-please-version
