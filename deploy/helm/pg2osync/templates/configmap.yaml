apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pg2osync.fullname" . }}
  labels:
    {{- include "pg2osync.labels" . | nindent 4 }}
data:
  {{- include "pg2osync.configData" . | trim | nindent 2 }}
